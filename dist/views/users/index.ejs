<%- include('../particles/header.ejs') %>

<div class="content">

    <div class="film">

        <% for (let i=0; i < users.length; i++) { %>

        <div class="users__object" id="users__object">
            <h2 class="users__name">
                имя: <%= users[i].name %>
            </h2>
            <h2 class="users__name">
                пароль: <%= users[i].password %>
            </h2>
        </div>
        </a>
        <form  action="/delete/users/<%= users[i].id %>" method="post">
            <button class="delete__users"><%- include('../icons/remove__icon__basket.ejs') %></button>
        </form>
        <% } %>
    </div>

</div>

<%- include('../particles/footer.ejs') %>