<%- include('../particles/header.ejs') %>
<div class="content">
    <div class="film">
        <% for (let i=0; i < bascet.length; i++) { %>

        <a href="/items/<%= bascet[i].id %>/show" class="descrip">
            <div class="film__object">
                <h2 class="film__name">
                    <%= bascet[i].name %>
                </h2>
                <div class="film__country">
                    <%= bascet[i].country %>
                </div>

                <div class="film__genre">
                    <%= bascet[i].genre %>
                </div>
                <img class="film__image" src="/img/<%= bascet[i].image %>">
                <div>
                    <form action="/delete__Video/<%= bascet[i].id %>" method="post">
                        <button class="delete__moves__bascet" type="submit"><%- include('../icons/remove__icon__bascet.ejs') %></button>
                    </form>
                </div>
            </div>
        </a>
        <% } %>
    </div>
</div>

<%- include('../particles/footer.ejs') %>